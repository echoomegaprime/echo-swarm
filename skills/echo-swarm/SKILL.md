---
name: echo-swarm
description: Convene GPT, Claude, Gemini, Grok, free or local models, the recovered Echo Swarm Brain, and the Maximalist Echo Fusion Worker inside the current chat; verify and return signed release-certificate graphics. Use for multi-model brainstorming, debate, building, review, validation, evidence-aware certification review, fused answers, certificate proof, and swarm status.
---

# Echo Swarm

Use the Echo Swarm MCP tools to make external model participation visible in the current chat.

1. Call `swarm_ping` before a provider-dependent run and report unavailable seats honestly.
2. Use `swarm_convene` for interactive collaboration. Select the smallest useful set of live seats and choose a purpose: `brainstorm`, `debate`, `build`, `review`, `validate`, `certify`, `plan`, or `report`.
3. Use `swarm_maximalist_start`, then poll `swarm_maximalist_result`, when the user requests fused output. Preserve dissent, uncertainty, provenance, and the returned run ID.
4. Use `swarm_brain_health`, `swarm_brain_think`, `swarm_brain_trinity_consult`, `swarm_brain_trinity_decide`, or `swarm_brain_hybrid` when the recovered Python brain contract is specifically useful.
5. Use `swarm_certificate_status` for official exact-release signature state and `swarm_certificate_artifact` for the printable page, SVG graphic, signed JSON, and independent verification link.
6. Never claim an advisory `certify` council response is official certification. Official status requires the certificate tool to verify the exact-SHA CertForge/GitHub App verdict and required digital signatures.
7. Never attempt to apply the Commander's signature through a model tool. That signature requires the visible private-edition GitHub OAuth and user-presence ceremony.
8. Do not request, print, or echo provider credentials. The private edition uses approved OAuth/session seats; the public edition accepts caller-owned API keys and cannot borrow private server credentials.

When returning results, identify contributing models, skipped seats, preserved dissent, unresolved uncertainty, and whether the answer came from the council, the recovered brain, or the Echo Fusion Worker.
